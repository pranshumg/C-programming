#include <stdio.h>

int main() {

    float x, y;
    printf("enter first number = ");
    scanf("%f", &x);
    printf("enter second number = ");
    scanf("%f", &y);

    printf("sum = %.2f\n", x + y);

    return 0;
}